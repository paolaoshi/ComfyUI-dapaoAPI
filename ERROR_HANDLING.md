# 妙笔AI常见报错中文说明与跨软件接入规范

版本：1.2  
整理日期：2026-09-13  
适用范围：接入妙笔AI的桌面软件、网站、插件、工作流、图像/视频生成和LLM应用。

本文整理自当前 ComfyUI-dapaoAPI 客户端的共享错误处理逻辑，并补充跨软件实现规范。用于指导客户端显示和处理错误，不是妙笔后端的完整错误码契约，也不是模型能力或计费承诺。示例均为虚构数据，不含真实API密钥、真实用户素材或私有后端源码。

妙笔默认API基础地址：`https://api.dapaoai.com`。具体接口、鉴权、模型参数和素材流程仍应结合对应的妙笔API接入文档；本文不替代这些文档。

## 1. 给开发者和开发助手的执行要求

1. 所有功能模块统一使用共享错误处理层，输出“具体中文说明 + 操作建议 + 原始错误”。只在英文前加“请求失败”或“生成失败”不算中文化。
2. 原始英文必须保留，不能被中文提示覆盖。原始信息是中文时直接保留，不得编造英文。展示、日志和导出前遮盖密钥等敏感信息。
3. HTTP错误、HTTP成功但业务失败、异步轮询失败、上传/下载失败、本地异常都要处理。
4. 抛异常、跳过错误、批量部分失败、返回错误JSON、流式错误事件均要走共享格式化。仅在最外层捕获异常，覆盖不到内部吞掉异常后返回字符串的路径。
5. 不得把正常模型输出、用户提示词或成功结果当作错误扫描和改写。
6. 报错说明不得改变请求协议、模型路由、输出结构和并发策略，也不得触发付费请求自动重试。
7. 未知错误必须说明原因尚不能确定，保留排查证据，不能默认归为余额、网络或审核问题。
8. 本文表格中“服务商”指当前请求实际使用的服务商；接入妙笔时可显示为“妙笔”。“节点名称”可替换成软件中的功能名称。

## 2. 用户界面展示格式

```text
操作：<图像生成 / 图像编辑 / LLM对话 / 视频生成 / 素材上传等>
模型：<实际发送的模型ID>
阶段：<素材上传 / 素材登记 / 提交任务 / 查询任务 / 下载结果 / 本地处理>
HTTP状态码：<有则显示，无则省略>
任务ID：<有则显示，无则省略>

中文说明：<具体原因与下一步建议>

原始错误（敏感信息已遮盖）：
<保留原始错误码、英文消息和字段路径>
```

不能把任务状态未知显示成生成失败。建议提供“复制错误详情”按钮，复制脱敏后的中文和原始错误，并保留实际模型ID、任务ID、发生时间及阶段。不要自动将完整用户素材、提示词和工作流打包上传给第三方。

## 3. 常见错误识别与中文提示

**本规范覆盖图像生成与编辑、LLM对话与提示词、视频生成、素材上传下载和本地处理，不限于视频。**

下表22类是基础场景速查。第12节是从项目代码直接导出的HTTP逐码说明和新增业务匹配目录，开发时以第12节及共享代码为准。第9、10节说明各功能如何处理错误；响应状态检测仍由各接口适配器负责，错误格式化函数不会从正常响应中臆造失败。未识别的新错误仍需保留原文。

下表为基础中文说明概括；同一个HTTP类别的具体状态码还会追加第12节中的逐码说明。为便于跨软件使用，将“ComfyUI”和“节点名称”分别改为“当前软件”和“功能名称”。匹配时忽略英文大小写，优先读取结构化错误码、状态码和错误消息。

| 类别 | 识别依据 | 中文提示正文 |
| --- | --- | --- |
| 疑似真人素材 | `may contain real person` | 上游认为输入图片可能包含真人，拒绝本次生成。请让服务商确认当前渠道是否支持这类素材；若并非真人，请提供任务ID核查误判。 |
| 内容审核 | `content_policy_violation`、`content safety policy`、`content_filter`、`safety filter`、`ResponsibleAIPolicyViolation`、`sensitive content`、`nsfw` | 上游内容审核未通过。请检查提示词和参考素材是否符合服务要求；若认为误判，请凭任务ID联系服务商核查。错误未指出具体内容时，无法确定触发原因。 |
| 素材不存在或过期 | 错误同时包含 `asset` 与 `not found` / `does not exist` / `expired` | 引用素材不存在或已失效。请让服务商核对素材ID、有效期及生成渠道是否一致，保留任务ID排查，避免重复创建素材或视频。 |
| 素材登记流程不支持 | `seedance_asset_model_unsupported` | 当前模型渠道不支持这条素材登记流程。请核对该模型应直传素材还是先登记素材，不能仅通过改模型ID解决。 |
| 上下文过长 | `context_length_exceeded`、`maximum context length`、`too many tokens` | 输入或预留输出超过模型上下文长度。请减少提示词、历史消息和参考资料，分段处理，或选择支持更长上下文的模型。 |
| 模型不支持 | `model_not_supported`、`unsupported model`、`model is not supported` | 当前接口或渠道不支持所选模型。请核对服务商提供的模型ID、接口协议和账号模型权限。 |
| 素材链接不可读 | `failed to download`、`failed to fetch image`、`invalid image url`、`url is not accessible` | 服务端无法读取参考素材。请检查素材链接是否可访问、是否过期，以及素材格式是否符合模型要求。 |
| 余额或额度 | HTTP 402；`insufficient_quota`、`insufficient balance`、`quota exceeded`、`insufficient credits` | 账号余额或可用额度不足。请检查所用服务商账号余额、API密钥额度和模型配额。 |
| 密钥认证 | HTTP 401；`invalid_api_key`、`invalid api key`、`incorrect api key`、`authentication_error` | 身份验证失败。请检查API密钥是否正确、是否过期，以及密钥与API地址是否属于同一服务商。 |
| 限流或排队 | HTTP 429；`rate_limit`、`rate limit`、`too many requests` | 请求受到限流或队列已满。请降低并发并稍后查询已有任务；提交结果不明时先查原任务，避免重复扣费。 |
| 服务端异常 | HTTP 500 / 502 / 503 / 504；`internal server error`、`bad gateway`、`service unavailable`、`gateway timeout` | 服务端异常、繁忙或上游响应超时。请保留任务ID，先查询已有任务状态；没有任务ID时联系服务商确认提交结果，再决定是否重新提交。 |
| 无访问权限 | HTTP 403；`permission denied` | 访问被拒绝。请检查账号、模型和素材的访问权限，并让服务商核查限制原因。 |
| 接口、模型或资源不存在 | HTTP 404；`model_not_found` | 接口、模型或请求的资源不存在。请核对API地址、模型ID和任务ID是否属于当前渠道。 |
| 素材大小或格式 | HTTP 413 / 415；`unsupported media` | 素材大小或媒体格式不符合接口要求。请核对文件大小、编码和格式，按当前模型要求处理后再提交。 |
| 参数错误 | HTTP 400 / 422；`invalid parameter`、`invalid_request_error`、`not valid`、`unsupported resolution` | 请求参数或素材不符合模型要求。请按下方原始错误中的字段，检查分辨率、时长、素材类型和数量。 |
| 请求或轮询超时 | HTTP 408；`timed out`、`timeout`、明确的超时异常（HTTP 504 优先归入服务端超时） | 请求或等待任务超时，不能据此判断生成失败。若已取得任务ID，请继续查询原任务；否则先确认网络和服务状态，避免重复提交付费请求。 |
| 网络、代理、DNS、TLS | `ConnectionError`、`connection refused`、`connection reset`、`ProxyError`、`SSLError`、`certificate verify failed`、`name resolution`、`getaddrinfo`、`cannot connect to host`、`max retries exceeded` | 网络连接、代理、DNS或TLS证书异常。请检查运行当前软件电脑到当前API地址的网络连接及代理设置；不能仅凭此错误确定故障在本机还是服务端。 |
| 响应解析失败 | `JSONDecodeError`、`expecting value`、`not json`、不是 JSON | 返回内容不是有效JSON，可能是错误页面或接口格式不匹配。请检查API地址，并保留原始响应交服务商核查。 |
| 本地内存或显存 | `out of memory`、`CUDA error` | 本地内存、显存或CUDA执行异常。请检查剩余内存和显存，减少本地批次或并发，并核对运行环境。 |
| 本地依赖或文件缺失 | `No module named`、`FileNotFoundError` | 本地依赖或文件缺失。请按原始错误检查当前软件使用的Python环境、所需依赖和文件路径。 |
| 未知英文错误 | 未匹配已知规则 | 执行失败，当前错误未匹配到已知原因，无法确定故障来源。请提供功能名称、模型ID、任务ID及下方原始错误供排查。 |
| 已有中文错误 | 未匹配专用规则，但原错误含中文；保留已有具体中文说明 | 请按下方原始错误中的中文提示检查。若仍无法解决，请提供功能名称、模型ID和任务ID供排查。 |

### 3.1 图片上传限流的补充提示

在图片上传阶段发生429或排队错误时，在原提示后追加：

> 若发生在图片上传阶段，请检查每张图片最长边是否超过2048px（2K），多图和批次也需逐张检查。

图片上传前，每张图片独立按比例缩放，最长边不超过2048px；默认使用高质量Lanczos缩放，尽量采用PNG编码。多图、批次都要在实际请求边界逐张预处理。2K是当前客户端预处理规范，不能仅凭429就断言图片超大；已经符合2K仍可能因并发、队列或服务端限制报错。

### 3.2 image-2、香蕉服务异常的补充提示

500、502、503、504或明确上游服务异常时，保留查询原任务的建议，并按实际产品提供的候选项追加：

- image-2：可在“模型”中选择“image-2官方稳定全分辨率”。
- 香蕉：可在“模型”中选择“香蕉pro官方稳定版”或“香蕉2官方稳定版”。

上述是现有软件的显示名称，不是可直接发送的模型ID。其他软件必须把名称映射到自己已经接入、且服务商确认可用的模型ID；未提供对应模型时不能显示一个无法执行的建议。不自动替用户切换模型或重新发起付费任务。

### 3.3 非标准443响应

> 收到非标准HTTP 443状态码，不能仅凭状态码判断原因。请检查代理和API地址，并让妙笔核查响应。

必须同时保留原始响应。HTTPS URL中的443是端口，不能当作HTTP错误码；消息包含HTTPS地址也不能证明故障一定来自本机。

### 3.4 真人素材、内容审核和素材登记不能混淆

- `may contain real person`：本次请求因疑似真人素材被拒绝，不能据此断言所有模型都不支持真人。
- `content_policy_violation`：审核未通过；上游未给出具体原因时，不猜测是哪一个词或哪一张图触发。
- `asset ... not found`：素材引用问题，不代表真人审核失败。
- `seedance_asset_model_unsupported`：所用素材登记流程不适用于当前模型渠道，需要核对对应接入文档。

不能把重新登记素材、改一个“真人模式”布尔值或无限重试作为审核拒绝的解决办法。`content[1]`等字段路径属于上游实际请求结构，不应在没有核对内容数组顺序时直接声称对应界面的“参考图1”。

## 4. 识别顺序与状态解析

### 4.1 推荐顺序

1. 判断这是否确实是错误事件，不检查正常文本中的关键词。
2. 从实际HTTP响应或异常对象取得HTTP状态码，从业务响应取得业务状态；不要从端口、任务ID或图像尺寸中抓取数字充当HTTP状态码。
3. 提取错误消息与错误码。兼容实际协议中的 `error.message`、`error.code`、`fail_reason`、`error_message`、`errorMessage` 等字段，以及 `data` 内的嵌套任务响应。
4. 优先匹配明确的业务原因，如真人拒绝、审核、素材失效、上下文超长、余额或密钥错误；再使用通用HTTP状态解释。例如HTTP400中有明确真人拒绝消息时，显示真人提示。
5. 原始错误不足以定位原因时，显示未知原因；中文提示与脱敏原文一起返回。

HTTP409按冲突给出核查建议，但不能仅凭409断言重复扣费或幂等键有误。未收录的状态码显示实际数值和未知原因，不强行套入已知类别。

### 4.2 HTTP成功不等于生成成功

以下是说明状态层级的示例，具体字段以对应妙笔接口为准：

```json
{
  "code": "success",
  "data": {
    "status": "FAILURE",
    "fail_reason": "The input image may contain real person."
  }
}
```

即使HTTP为200、外层 `code` 为 `success`，内部视频任务仍然可能失败。外层 `message: success` 不能覆盖内部 `fail_reason`。提交返回 `queued` 只表示已接收；中转队列完成也不等于模型视频生成成功。

不要把 `result_url` 字段里的英文错误当成视频URL。字段名不是内容有效性的保证；只有任务成功且取得有效结果引用后才按协议获取成品。

## 5. 跨软件共享错误对象与伪代码

下面是建议的软件内部格式，不是妙笔服务器的请求或响应契约：

```json
{
  "category": "real_person_rejected",
  "message_zh": "上游认为输入图片可能包含真人，拒绝本次生成。",
  "suggestion_zh": "请让妙笔确认当前渠道是否支持这类素材；若并非真人，请凭任务ID核查误判。",
  "raw_error_redacted": "The input image may contain real person.",
  "http_status": 200,
  "model": "<actual-model-id>",
  "stage": "video_poll",
  "task_id": "<task-id>",
  "allow_automatic_paid_retry": false
}
```

其中HTTP200表示任务查询HTTP请求成功，`category`表示查到的业务失败，两者不矛盾。

```text
handle_error(actual_exception_or_failed_response, request_context):
    http_status = read_actual_http_status()
    business_error = extract_actual_failure_message_and_code()
    raw = redact_secrets(business_error)
    category, chinese_hint = classify(business_error, http_status, request_context.stage)
    return normalized_error(chinese_hint, raw, request_context)

execute_feature():
    try:
        result = call_existing_protocol()
        if result_is_business_failure(result):
            return_or_raise(handle_error(result, context))
        return result
    catch error:
        return_or_raise(handle_error(error, context))
```

`return_or_raise`沿用功能原有错误策略；跳过失败时仍保留输出类型和数量。批量任务按每一项保存失败原因。取消操作不能变成普通生成失败；公共错误函数不得承担网络请求、补传素材或任务重提。

现有ComfyUI实现使用 `node_error_utils.format_node_error` 作为共享格式化函数，`node_execution_gate` 在注册节点执行入口兜底；其他语言可按本文独立实现，不依赖ComfyUI环境。建议内部用结构化“已格式化”标记防止重复包装，避免仅凭上游文本中出现某个展示标签就跳过处理。

## 6. 原始错误、脱敏与安全保存

- 保留错误码、英文消息、出错字段路径、HTTP状态码、任务ID；有多个状态层级时分别标注。
- 遮盖Authorization、Bearer凭据、API Key、token、secret、password，以及链接中携带的访问凭据。不能仅遮盖 `sk-` 前缀密钥，其他格式的密钥也要处理。
- 应用知道实际密钥时应直接遮盖其精确值；结构化字段脱敏优先，正则用于补充。
- 展示脱敏错误不意味着底层任意堆栈或原始日志都已脱敏；错误弹窗、日志、复制按钮、批量导出和JSON响应分别检查。
- 不把密钥、真实用户工作流、原始请求素材、私有服务端代码写入公开仓库。
- 过长响应可在界面折叠或展示脱敏摘要，明确说明已截断；必要的完整脱敏详情由用户本地保存，不静默丢掉关键原因。

## 7. 超时、重试和计费

- 错误格式化函数本身不进行任何重试。
- 付费生成POST不得因超时、500、502、503、504或429而自动重复提交。
- 提交结果未知时，优先通过原任务ID、作业ID或接口正式支持的恢复方式确认结果。不能因查询失败就创建新任务。
- 只读查询可按接口要求有限重试和退避；上传、登记等写操作按其具体协议处理，不将生成重试规则直接套用。
- 不凭HTTP状态码、失败状态或单个 `quota: 0` 字段宣称“没扣费”“已退款”；实际账务以服务商账单为准。

## 8. 离线验收清单

- [ ] 模拟表格内所有常见错误，中文原因正确且原始英文仍存在。
- [ ] 验证HTTP200包裹业务失败、异步排队完成但生成失败。
- [ ] 验证抛异常、跳过错误、批量部分失败、返回错误JSON和流式错误路径。
- [ ] 验证任务ID、实际模型ID、字段路径、异常属性不丢失。
- [ ] 验证API Key、Bearer、带凭据URL在显示、日志和导出中均被遮盖。
- [ ] 验证超时与服务异常不会新增付费POST，查询失败不会重建任务。
- [ ] 验证正常文本包含“HTTP500”“nsfw”等词时不会被当成报错处理。
- [ ] 验证任务ID中的数字、HTTPS443端口、图片尺寸不会误判为状态码。
- [ ] 验证重复经过错误处理不会多次堆叠中文说明。
- [ ] 验证本地中文参数校验保留原文，不编造上游英文。

本清单是跨软件实现的验收要求，不代表其他软件已经通过验证。新增错误案例应使用虚构数据或脱敏模拟响应进行测试；无需发起真实付费任务来验证提示文案。


## 9. 429专项：按具体原因区分，不能一律归为余额不足

以下英文是用于适配和测试的典型语义示例，不是妙笔已确认的完整错误码清单。先读取实际错误码、消息和限制维度；证据不足时使用第9.9节通用提示，不推断是哪一种限额。

| 场景 | 需要观察的实际证据 / 英文语义示例 | 建议中文提示 | 下一步处理 |
| --- | --- | --- | --- |
| 9.1 请求次数过频 | 明确提及 requests per minute、RPM、request rate limit | 请求频率超过当前限制，请减少单位时间内的请求次数。 | 分散请求发送时间，检查多个页面、后台任务是否共用同一账号或Key。 |
| 9.2 并发任务超限 | 明确提及 concurrent requests、concurrency limit、too many active tasks | 同时运行的任务数超过限制，请等待已有任务完成后再发起新任务。 | 降低并发数；只降低每秒发送量可能仍有过多在途任务。 |
| 9.3 LLM Token速率超限 | 明确提及 tokens per minute、TPM、token rate limit | 当前Token处理速率超过限制，请减少同时处理的长文本或降低请求频率。 | 缩短不必要的历史与参考资料、降低并发；不要把它误报成单条上下文超长。 |
| 9.4 日配额等周期额度耗尽 | 明确指出 daily quota、requests per day、RPD或具体周期配额 | 当前周期可用配额已用尽，请检查配额和重置时间。 | 有明确重置时间才显示；不能仅凭quota一词断言需充值或多久恢复。 |
| 9.5 明确余额或Key额度不足 | insufficient balance、insufficient credits，或明确说明余额/额度的结构化错误 | 账号余额或API密钥可用额度不足，请检查余额和密钥额度设置。 | 依据具体字段区分余额、Key额度和模型配额，不能只因HTTP429就要求充值。 |
| 9.6 队列已满 | 明确提及 queue full、queue capacity exceeded | 当前任务队列已满，请减少提交数量，等待队列释放后再提交新任务。 | 已取得任务ID的请求继续查原任务，不重复排队。 |
| 9.7 上游容量不足 | 明确提及 overloaded、capacity exhausted、no available capacity | 当前模型服务容量不足，请稍后使用，或选择已接入的其他可用模型。 | 不自动替用户切换模型和扣费；没有明确上游证据时不强行归因上游。 |
| 9.8 上传或查询接口限流 | 结合请求阶段：upload、素材上传、poll、状态查询 | 素材上传或任务查询过于频繁，请降低该环节的并发或查询频率。 | 上传图片另检查逐张2K；轮询限流时延长查询间隔，不能重新生成。 |
| 9.9 仅有429，无详细原因 | Too Many Requests或缺少细节的rate limit | 请求受到限制，当前响应未明确说明是频率、并发、配额还是服务容量问题。请降低频率并保留原始错误交妙笔核查；已有任务请先查询状态。 | 不猜测余额不足、素材过大或固定恢复时间。 |

### 9.10 开发实现注意事项

- 429细分优先级：明确错误码与限制维度优先于HTTP429通用提示。不能让通用429分支提前吞掉TPM、并发、队列等明确原因。
- `insufficient_quota` / `quota exceeded` 单独出现时只能说明额度或配额问题，未必代表账户余额不足；没有详情时提示用户核查具体额度来源。
- 按实际协议解析限流响应中的 `Retry-After` 或重置字段；没有这些字段时不编造等待秒数。它们是调度线索，不构成重发付费请求的授权。
- 只读查询采用有上限的退避和适当随机抖动，避免所有任务同时再次查询；保留查询尝试次数和最后错误。
- 统计账号、Key、模型、功能及请求阶段的并发和请求量，检查前端重复点击、后台重复轮询和多个实例共用Key。
- 降低并发与减小图片是不同措施。2K图片规范用于实际上传预处理，不能作为所有429的统一解释。
- 对流式LLM，即使最初HTTP连接成功，后续流中出现错误也应显示中文和原文；不能把不完整文本标记为完整成功，不能悄悄重发整条付费请求。

## 10. 各功能补充错误场景

本节列出各功能的处理要求；第12节列出的英文匹配已进入共享代码。只有实际错误和业务阶段支持该判断时才使用；空结果、流式中断等是否构成失败仍须由对应接口适配器判定，不能扫描正常文本任意猜测。

| 功能 | 场景 / 证据 | 建议中文提醒 |
| --- | --- | --- |
| 图像生成、编辑 | 成功响应中没有图像结果 | 接口已返回响应，但未找到有效图片。请核对响应协议并保留响应详情，不自动重新生成。 |
| 图像生成、编辑 | 无效图像字节、Base64解析失败 | 返回的图像数据无效或不完整，请保留响应信息核查；不能将占位图当作生成成功。 |
| 图像生成、编辑 | 明确分辨率、比例、批次数量不支持 | 当前模型不支持所选图像参数，请按原始错误检查分辨率、比例或数量。 |
| 图像编辑 | 明确蒙版或尺寸不匹配 | 编辑图片与蒙版不匹配，请检查尺寸、通道和格式；缩放图片后须保持蒙版对齐。 |
| LLM | 明确不支持图片、音频、视频输入 | 当前模型或接口不支持这种输入类型，请使用已接入的对应多模态模型，或去掉不支持的素材。 |
| LLM | context length或输出长度限制 | 输入或预留输出超出限制，请减少输入、分段处理或调整输出预算；这与每分钟Token限流不同。 |
| LLM | 明确输出被长度限制截断 | 模型输出因长度限制被截断，请调整任务范围或输出预算。保留已返回内容，不把不完整结果标记为完成。 |
| LLM | 空正文、只返回工具调用等 | 未取得当前功能需要的正文，请核对响应结构和工具调用处理；不能直接断言模型未执行。 |
| LLM | SSE/流式连接中途断开 | 流式响应中断，结果可能不完整。请保留已接收内容和请求标识，确认状态后再决定后续操作。 |
| 上传、下载 | 链接过期、无访问权限、403/404 | 素材或结果链接可能失效或无权限，请检查具体响应，必要时通过原任务重新取得有效链接。 |
| 上传、下载 | 明确编码、媒体格式、文件大小错误 | 文件不符合接口要求，请按错误中指出的格式、编码或大小限制处理。 |
| 上传、下载 | 下载到HTML/JSON错误页 | 获取到的内容不是预期媒体文件，请保留响应类型和错误内容核查，不按图片或视频强行解码。 |
| 通用接口 | 返回HTML而不是JSON | 接口返回了非预期内容，请检查API地址和协议，并让妙笔核查网关响应。 |
| 通用接口 | 明确方法不允许、Content-Type不支持 | 请求方法或内容类型不符合接口要求，请按该接口文档核对，不自动更改方法重新提交。 |
| 通用接口 | 重复请求或幂等冲突 | 本次请求与已有请求冲突，请核对原任务和幂等标识。不要直接换新标识重发付费任务。 |
| 通用接口 | 明确模型ID、路由不存在 | 所选模型或接口路由不可用，请核对妙笔提供的实际模型ID和接口路径。 |
| 本地处理 | 磁盘空间不足、文件写入失败 | 本地结果保存失败，请检查目标目录权限和剩余空间。生成任务可能已成功，应优先恢复下载或保存。 |
| 本地处理 | 依赖缺失、编码器不可用 | 本地运行环境缺少所需组件，请按原始错误安装或配置对应依赖。 |
| 视频 | 时长、参考素材数量或角色不支持 | 素材或参数不符合当前视频模型要求，请核对原始错误指出的字段和对应模型文档。 |

这些情况也必须输出原始英文、实际模型ID和阶段。没有任务ID的同步图像或LLM请求可记录接口返回的请求标识；接口未返回标识时注明未取得，不编造ID。

## 11. 新增验收用例

- [ ] 图像、LLM、上传、下载、轮询各自模拟429，不只测试视频生成。
- [ ] 分别模拟RPM、TPM、并发、周期配额、余额、队列满、容量不足，以及无细节的429。
- [ ] 只有明确余额证据时显示余额提醒；只有明确重置时间时显示该时间。
- [ ] 模拟同步请求失败、HTTP200内部失败、流式中途失败和本地保存失败。
- [ ] 本地保存失败不会重新生成；查询限流不会重建任务；流式中断不会偷偷重新付费。
- [ ] 通用提示不覆盖具体上游原因；未知错误仍能复制脱敏后的完整排查信息。


## 12. 项目已实现的逐码目录（由代码生成）

本版共享目录包含 **43 个HTTP/网关状态码**和 **17 组新增业务匹配**，并保留第3节的审核、素材、网络等基础规则。来源：`error_message_catalog.py`；执行入口：`node_error_utils.format_node_error`。所有注册节点有异常兜底，跳过错误和批量失败路径显式调用同一函数。

优先展示具体业务原因，同时补充HTTP层说明，原始错误另行保留。以下是客户端排查建议，不是妙笔保证会返回这些状态的承诺。402按接口原文判断支付或配额含义；520—526属于非标准网关语义，须确认实际网关后再采用Cloudflare解释。

### 12.1 HTTP逐码中文说明

| 状态码 | 中文原因及处理建议 |
| --- | --- |
| 400 | 请求参数或素材不符合接口要求。请检查原始错误指出的字段、必填项、JSON结构和模型参数。 |
| 401 | 身份验证失败。请核对API密钥、有效期，以及密钥与API地址是否属于同一服务商。 |
| 402 | 接口要求检查支付或可用额度。请结合原始错误核查账号余额、密钥额度和模型配额；不能仅凭状态码判断实际扣费。 |
| 403 | 访问被拒绝。请核对账号、模型、素材权限和服务商限制；不要直接认定密钥错误或余额不足。 |
| 404 | 接口、模型或请求的资源不存在。请检查API基础地址、接口路径、实际模型ID及任务或素材ID是否属于当前渠道。 |
| 405 | 接口不接受当前HTTP请求方法。请按接入文档核对GET、POST等方法和接口路径，不自动换方法重发。 |
| 406 | 接口无法提供客户端要求的响应格式。请检查Accept请求头和接口支持的返回格式。 |
| 407 | 代理服务器要求身份验证。请检查代理账号和代理配置；这不等同于妙笔API密钥无效。 |
| 408 | 服务端等待接收请求超时。请检查网络和上传过程；提交结果未知时先核查原任务，不重复提交付费请求。 |
| 409 | 请求与当前资源状态冲突。请核查已有任务、资源版本及幂等标识；不能直接换新标识重复生成。 |
| 410 | 请求的资源已不再可用。请核查素材或结果有效期，优先通过原任务取得新的有效引用。 |
| 411 | 接口要求提供请求内容长度。请检查上传方式与Content-Length，由HTTP客户端正确计算。 |
| 412 | 请求的前置条件不满足。请检查资源版本及If-Match等条件头，核对最新状态后再处理。 |
| 413 | 素材大小或请求体超过接口限制。请减少文件体积或批次数量，并核对该模型允许的上传大小。 |
| 414 | 请求URL过长。请检查是否将长文本或素材放入URL参数，按接口要求使用请求体传递。 |
| 415 | 媒体格式或内容类型不受支持。请检查Content-Type、文件实际格式和编码，不能只修改文件后缀。 |
| 416 | 请求下载的字节范围无效。请核查文件长度和Range参数，修正断点续传；无需重新生成。 |
| 417 | 服务端不能满足Expect请求头要求。请核对HTTP客户端和代理的请求头配置。 |
| 421 | 请求被发送到无法处理该目标的服务。请检查API域名、Host、代理路由与连接配置。 |
| 422 | 请求结构可读取，但参数或素材无法按要求处理。请检查具体字段值、参数组合和素材限制。 |
| 423 | 资源处于锁定状态。请检查是否有其他操作占用，按接口规则等待或处理锁状态。 |
| 424 | 本操作依赖的前序操作失败。请先检查前序任务或素材处理结果，不只重试当前步骤。 |
| 425 | 服务端拒绝可能被重放的过早请求。请检查客户端或代理的早期数据设置，并核查原请求状态。 |
| 426 | 服务端要求升级通信协议。请检查接口要求、客户端版本和代理支持的协议。 |
| 428 | 请求缺少服务端要求的前置条件。请按接口文档补齐版本或条件头，避免覆盖已变化的资源。 |
| 429 | 请求受到限流或配额限制，具体维度需看原始错误。请检查频率、并发、配额和队列；已有任务先查询，不自动重提。 |
| 431 | 请求头过大。请检查重复请求头、过长Cookie和凭据字段，减少无关头部。 |
| 451 | 资源因法律要求而不可用。请查看原始说明并联系服务商确认适用限制。 |
| 500 | 服务端内部异常。请保留任务ID和原始错误交服务商排查；提交结果未知时先核查原任务。 |
| 501 | 服务端未实现处理本请求所需的功能。请核对接口路径、HTTP方法和当前渠道支持的能力，由服务商确认；反复重试通常不能解决功能缺失。 |
| 502 | 网关从上游收到无效响应。请保留任务ID让服务商核查上游连接与响应；不能直接归因为本机网络问题。 |
| 503 | 服务暂时不可用，可能繁忙或维护。请查看服务状态和响应中的等待提示，先确认已有任务再安排后续请求。 |
| 504 | 网关等待上游响应超时。生成可能仍在进行，请继续查询原任务或让服务商确认，不自动重复生成。 |
| 505 | 服务端不支持当前HTTP协议版本。请检查HTTP客户端、代理及接口要求的协议版本。 |
| 507 | 服务端存储空间不足。请联系服务商核查存储容量；这不等同于用户本地磁盘已满。 |
| 511 | 当前网络要求先完成接入认证。请检查网络登录页面或网关认证；这不等同于妙笔API密钥错误。 |
| 520 | 收到非标准网关错误520；在Cloudflare语义下表示源站返回未知异常。请让服务商核查实际网关及源站日志。 |
| 521 | 收到非标准网关错误521；在Cloudflare语义下表示源站拒绝连接。请让服务商核查源站服务和防火墙。 |
| 522 | 收到非标准网关错误522；在Cloudflare语义下表示连接源站超时。请让服务商核查源站连接。 |
| 523 | 收到非标准网关错误523；在Cloudflare语义下表示源站不可达。请让服务商核查源站地址和路由。 |
| 524 | 收到非标准网关错误524；在Cloudflare语义下表示连接建立后等待响应等过程超时。请保留原任务，核查状态。 |
| 525 | 收到非标准网关错误525；在Cloudflare语义下表示网关与源站TLS握手失败。请让服务商核查源站TLS配置。 |
| 526 | 收到非标准网关错误526；在Cloudflare语义下表示源站证书无效。请让服务商核查证书；不要关闭客户端证书校验。 |

### 12.2 新增业务匹配目录

这些规则只匹配实际失败消息。具体业务提示在前，HTTP说明在后，二者都保留。

| 类别 | 英文匹配 | 中文原因及处理建议 |
| --- | --- | --- |
| `token_rate` | `tokens per minute`、`token rate limit`、`tpm limit`、`tpm exceeded` | Token处理速率超过限制。请降低并发、减少同时发送的长文本和历史记录；这与单条上下文长度超限不同。 |
| `concurrency` | `concurrency limit`、`concurrent requests`、`too many active tasks`、`max concurrency` | 同时运行的任务超过并发限制。请等待已有任务结束，并减少同时提交的任务数。 |
| `request_rate` | `requests per minute`、`rpm limit`、`rpm exceeded` | 单位时间内请求次数超过限制。请分散发送时间，检查多个页面或实例是否共用同一密钥。 |
| `period_quota` | `daily quota`、`requests per day`、`rpd limit`、`daily limit` | 当前周期配额已用尽。请检查配额及明确返回的重置时间；短时间反复请求不能恢复额度。 |
| `queue_full` | `queue full`、`queue is full`、`queue capacity exceeded` | 当前任务队列已满。请减少新任务提交，等待队列释放；已取得任务ID的请求继续查询原任务。 |
| `capacity` | `overloaded`、`capacity exhausted`、`no available capacity` | 当前服务处理容量不足。请保留原任务信息，稍后使用或手动选择已经接入的其他可用模型。 |
| `quota` | `insufficient_quota`、`quota exceeded` | 可用额度或配额不足，当前信息不一定表示账户余额不足。请核查密钥额度、模型配额及具体限制维度。 |
| `unsupported_input` | `does not support image`、`does not support audio`、`does not support video`、`unsupported input type` | 当前模型或接口不支持这种输入类型。请核对图片、音频或视频能力，选择已接入的对应多模态模型。 |
| `truncated_output` | `output truncated`、`max_tokens exceeded`、`finish_reason: length` | 模型输出因长度限制被截断。请减少单次任务范围或调整输出预算，保留已有内容，不将其标记为完整成功。 |
| `empty_output` | `empty response`、`empty completion`、`no image returned`、`no images returned` | 未取得当前功能需要的有效结果。请核对响应结构和模型输出类型，保留响应信息，不自动重新生成。 |
| `invalid_image` | `invalid image data`、`cannot identify image file`、`incorrect padding`、`invalid base64` | 图像或Base64数据无效或不完整。请核对媒体内容和编码，不把错误页面或占位图当作生成成功。 |
| `mask_mismatch` | `mask size mismatch`、`mask and image must`、`mask dimensions` | 蒙版与编辑图片不匹配。请检查尺寸、通道和格式，缩放图片后保持蒙版对齐。 |
| `stream_interrupted` | `chunkedencodingerror`、`response ended prematurely`、`incomplete read`、`incompleteread`、`stream interrupted` | 响应传输中断，内容可能不完整。请保留已接收内容和请求标识，核查状态；不要自动重发整条付费请求。 |
| `disk_full` | `no space left on device`、`disk full`、`磁盘空间不足` | 本地磁盘空间不足，保存结果失败。请清理空间并恢复保存或下载；生成可能已成功，不要重新生成。 |
| `local_permission` | `permissionerror`、`winerror 5`、`errno 13` | 本地文件访问被拒绝。请检查目标目录权限、文件占用和保存路径；保存失败不代表生成失败。 |
| `codec` | `unknown encoder`、`encoder not found`、`ffmpeg not found`、`ffprobe not found` | 本地媒体处理组件或编码器缺失。请检查ffmpeg、ffprobe及所需编码器的安装与路径。 |
| `redirect` | `toomanyredirects`、`too many redirects` | 请求发生过多重定向。请检查API或素材URL及代理配置，不把认证信息转发到未知目标。 |

### 12.3 状态码语义参考

客户端操作建议按项目实际接入需求整理。HTTP基础语义参照 [RFC 9110](https://www.rfc-editor.org/rfc/rfc9110.html)；428、429、431、511参照 [RFC 6585](https://www.rfc-editor.org/rfc/rfc6585.html)；423、424、507参照 [RFC 4918](https://www.rfc-editor.org/rfc/rfc4918.html)；451参照 [RFC 7725](https://www.rfc-editor.org/rfc/rfc7725.html)。非标准520—526参照 [Cloudflare网关错误说明](https://developers.cloudflare.com/support/troubleshooting/http-status-codes/cloudflare-5xx-errors/)。

### 12.4 防止代码与文档脱节

- 增加HTTP状态或业务错误时，同步修改共享目录、模拟测试和本文对应表格。
- 文档中的推荐处理流程不授权自动重试；实际代码仅格式化错误。
- 常见码逐项测试，不仅测试429；同时测试每个业务规则及原始消息保留。
- 全部离线验证，不需要以真实付费请求触发服务错误。
